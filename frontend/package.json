{
  "name": "dice-goblins-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "npm exec vitest -- run",
    "test:watch": "npm exec vitest --",
    "preview": "vite preview",
    "db:schema": "node ./scripts/build-schema-all.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "strip-comments": "^2.0.1",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
